services:
  app:
    image: ghcr.io/ahmadasgf/my-nestjs-app:latest 
    container_name: app-nest
    ports:
      - "3000:3000"
    restart: always
    networks:
      - backend-net

networks:
  backend-net:
    external: true
